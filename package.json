{
  "name": "nerf-watch",
  "version": "0.1.0",
  "private": true,
  "description": "Checks whether an AI model has gotten worse since you started watching it.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "nerf": "node --env-file-if-exists=.env src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
